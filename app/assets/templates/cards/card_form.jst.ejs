<% if (!card.id) { %>
  <h4>Create a new Card</h4>
<% } else { %>
  <h4>Update Card</h4>
<% } %>

<label>Card Title
  <input type="text" name="card[title]" value="<%= card.escape("title") %>">
</label>

<label>Card description
  <textarea
    name="card[description]"
    rows="4"
    cols="20"><%= card.escape("description") %>
  </textarea>
</label>

<button class="new-card">Submit Card</button>
